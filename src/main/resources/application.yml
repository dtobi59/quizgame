spring:
  datasource:
    url: jdbc:mysql://localhost:3306/javaquiz
    username: root
    password: null
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      database: mysql
      database-platform: org.hibernate.dialect.MySQL57InnoDBDialect